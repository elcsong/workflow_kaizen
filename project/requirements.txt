# Intent Matcher 필수 패키지

# 기본 패키지 (필수)
pandas>=1.3.0
pyyaml>=5.4.0
numpy>=1.20.0
openpyxl>=3.0.0

# SBERT 의미 분석 패키지 (선택사항 - 의미 분석 사용 시 필요)
# sentence-transformers>=2.0.0
# torch>=1.9.0
# transformers>=4.0.0

# 참고: SBERT 패키지는 --use-sbert 옵션 사용 시에만 필요합니다.
# 설치 시 주석을 해제하고 설치하세요:
# pip install sentence-transformers torch transformers

